langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_text
    - node.type.card
id: node.card.field_text
field_name: field_text
entity_type: node
bundle: card
label: 'Rules text'
description: 'Plain text. May contain {i}/{e} icon markers — parsed and rendered to inline SVG at display time.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  case_sensitive: false
field_type: string_long
