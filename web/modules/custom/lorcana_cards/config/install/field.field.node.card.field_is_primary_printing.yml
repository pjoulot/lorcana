langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_is_primary_printing
    - node.type.card
id: node.card.field_is_primary_printing
field_name: field_is_primary_printing
entity_type: node
bundle: card
label: 'Is primary printing'
description: 'Exactly one printing per group is flagged primary; default listings/search return only primaries.'
required: true
translatable: false
default_value:
  - value: 0
default_value_callback: ''
settings: {  }
field_type: boolean
