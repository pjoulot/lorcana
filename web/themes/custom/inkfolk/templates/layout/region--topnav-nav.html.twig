{#
/**
 * @file
 * Transparent wrapper for the topnav navigation region.
 *
 * The design's topnav grid expects the menu's <ul> to sit directly
 * under .if-topnav__left so flex spacing works; suppress Drupal's
 * default <div> region wrapper.
 */
#}
{{ content }}
