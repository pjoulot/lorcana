{#
/**
 * @file
 * Transparent wrapper for the footer-menu region.
 *
 * The design's footer grid expects each menu column to be a direct
 * child of .if-footer__cols (alongside the brand column); suppress
 * Drupal's default <div> region wrapper.
 */
#}
{{ content }}
