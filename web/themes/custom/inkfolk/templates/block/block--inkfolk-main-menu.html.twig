{#
/**
 * @file
 * Transparent block wrapper for the topnav main-menu block.
 *
 * The accompanying menu--main.html.twig emits the <nav>/<ul> with
 * design classes; we don't want the default nav/heading wrapper
 * here on top of it.
 */
#}
{{ content }}
