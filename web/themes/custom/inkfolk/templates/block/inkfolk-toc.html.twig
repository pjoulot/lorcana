{#
/**
 * @file
 * "On this page" auto-TOC container. js/toc.js injects the link list, built
 * from the page's .if-rt__body headings, and adds scroll-spy highlighting.
 */
#}
<nav class="if-toc" aria-label="{{ heading }}">
  <div class="if-toc__label">{{ heading }}</div>
</nav>
